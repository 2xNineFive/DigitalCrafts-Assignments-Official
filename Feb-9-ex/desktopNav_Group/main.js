console.log('Hello World');

// Somehow we need to populate the header onto the screen. And We're gonna do this using Javascript and the DOM. 

// Once we've populated the header onto the DOM, we need to style it using CSS. 